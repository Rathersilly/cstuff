src = 'test_vector.cpp'
cc = 'g++'
cc = 'clang++'
# opts = '-fno-elide-constructors'
opts = '-std=c++20'
outfile = '-o a.out'

desc 'build test_vector.cpp'
task :default do
  sh "#{cc} #{outfile} #{src} #{opts} -g3 -O0"
end
